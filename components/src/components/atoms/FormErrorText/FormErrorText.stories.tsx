import { FormErrorText } from '@/components';

export default {
  title: 'Components/Atoms/Form Error Text',
  decorators: [],
};

export const Default = () => {
  return <FormErrorText>Error</FormErrorText>;
};
